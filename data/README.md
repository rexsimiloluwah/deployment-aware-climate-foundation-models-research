# Data Directory

Real dataset files live here, but large data should not be committed.

Use these manifest templates:

- `data/western_kenya_crop_type/manifest.example.csv`
- `data/ftw_africa/manifest.example.csv`

Copy each template to `manifest.csv` after placing the real arrays/rasters under the dataset folder.

